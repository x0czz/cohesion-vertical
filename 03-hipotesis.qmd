---
title: "Hipótesis"
---

H1:
$$
c_{05_{ti}} = \gamma_{00} + \sum_{g=2}^{5} \gamma_{g0}\,\text{Gen}_{gi} + u_{0i} + \varepsilon_{ti}
$$

donde $\text{Gen}_{gi}$ son variables indicadoras para cada cohorte $g$ (ref. = Silent), y se espera que

$$
\gamma_{20} < \gamma_{30} < \gamma_{40} < \gamma_{50} < 0
$$

H2:Clase social.

$$
c_{05_{ti}} = \gamma_{00} + \sum_{k=2}^{7} \gamma_{k0}\,\text{EGP}_{ki} + u_{0i} + \varepsilon_{ti}
$$

donde $\text{EGP}_{ki}$ son variables indicadoras (dummy) para cada categoría $k$ de la escala EGP-7 (categoría de referencia = Service Class I), y se espera que

$$
\gamma_{k0} > 0
$$

para las clases manuales y trabajadoras, reflejando mayores niveles de cohesión vertical en comparación con la clase de servicio.

H3: Identificación nacional.

$$
c_{05_{ti}} = \gamma_{00} + \gamma_{10}\,\text{c32}_{ti} + u_{0i} + \varepsilon_{ti}
$$

donde $\text{c32}_{ti}$ es el nivel de identificación nacional del individuo $i$ en el tiempo $t$, y se espera que

$$
\gamma_{10} > 0
$$

Ecuacion final con controles:

Fragmento de código

$$
\begin{aligned}
c_{05_{ti}} = \, & \gamma_{00} + \gamma_{10}\,\text{c38}_{i} + \gamma_{20}\,\text{edad}_{i} + \gamma_{30}\,\text{trato}_{i} + \gamma_{40}\,\text{c15}_{i} + \gamma_{50}\,\text{c32}_{ti} + \gamma_{60}\,\text{Sexo}_{i} + \gamma_{70}\,\text{Nedu}_{i} + \\
& \sum_{g=2}^{5} \gamma_{g0}\,\text{Gen}_{gi} 
+ \sum_{k=2}^{7} \gamma_{k0}\,\text{EGP}_{ki} 
+ \sum_{w=1}^{3} \gamma_{w0}\,\text{Ola}_{wti} 
+ u_{0i} + \varepsilon_{ti}
\end{aligned}
$$

